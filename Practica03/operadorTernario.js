//condicion ? exp1 : exp2
// exp1 se ejecuta cuando es verdadera 
//exp2 se ejecuta cuando es falsa

let resultado = 3 > 2 ? "Verdadero" : "Falso" ;

console.log(resultado);

let numero = 9;

resultado = (numero % 2 == 0) ? "Numero par" : "Numero impar";
console.log(resultado);
